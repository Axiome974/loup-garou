<%- include("./partial/header.ejs") %>

<div class="container-fluid container-lg">

    <div class="row pt-2">
        <div class="col-4 d-grid">
            <a href="?manage=players" class="btn btn-success btn-lg">Joueurs</a>
        </div>
        <div class="col-4 d-grid">
            <a href="?manage=game" class="btn btn-success btn-lg">Partie</a>
        </div>

        <div class="col-4 d-grid">
            <a href="?manage=admin" class="btn btn-warning btn-lg">Admin</a>
        </div>
    </div>
    <hr>

    <div class="row mt-3">
        <div class="col-12">

            <% if( manage === "game" ) { %>

                <%- include("./partial/game_remote.ejs")%>

            <% } else if (  manage === "players" ) { %>

                <%- include("./partial/players_remote.ejs", {players, admin:false} )%>

            <% } else if (  manage === "admin" ) { %>

                <%- include("./partial/players_remote.ejs", {players, admin:true} )%>

            <% } %>

        </div>

    </div>

</div>


<%- include("./partial/footer.ejs") %>
